const assert = require('assert');

describe('db', () => {
  describe('setupDB', () => {
    it('should setup the database correctly', () => {
      // test code
    });
  });

  describe('addTweet', () => {
    it('should add a tweet to the database', () => {
      // test code
    });
  });

  describe('listTweets', () => {
    it('should list tweets from newest to oldest', () => {
      // test code
    });

    it('should prioritize elonmusk tweets', () => {
      // test code
    });
  });
});